// theme.js
import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#f27500',
    },
    background: {
      default: '#f5f5f5',
    },
  },
});

export default Theme;
